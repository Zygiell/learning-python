from math import pi


def pow_kwadrat(a):
    return (a * a)


def pow_prostok(a, b):
    return (a * b)


def pow_triangle(a, h):
    return ((a * h)/2)


def pow_trapez(a, b, h):
    return (((a + b) * h)/2)


def pow_kola(r):
    return(pi*r**2)
